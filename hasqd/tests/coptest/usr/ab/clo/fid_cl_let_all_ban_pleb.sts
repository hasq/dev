#!/bin/sh

# CLO let=all ban=pleb

in=$(basename $0)
in=${in%.*} 

$1/hasqd -cqnx lock=1 dprn=1 dsvt=1 tcp_port=14000 s=$in.inp webroot="webroot" skckey="key" let="all" ban="pleb" | grep -v "<svt> sendnote\|<svt> conupdate\|<svt> reorg\|<svt> noterecv\|<svt> chkconfl\|<svt> setconfl\|<svt> conflict"  


